{
  "name": "aura-silhouette",
  "version": "1.0.0",
  "description": "Interactive Three.js body-scan visualization and torso analysis.",
  "scripts": {
    "dev": "parcel src/index.html",
    "build": "node scripts/clean-build.mjs && parcel build src/index.html --dist-dir dist/client --public-url / --no-source-maps && node scripts/create-worker.mjs"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "module",
  "dependencies": {
    "three": "^0.184.0"
  },
  "devDependencies": {
    "parcel": "^2.16.4"
  }
}
